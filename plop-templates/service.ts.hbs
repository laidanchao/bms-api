import { Injectable } from '@nestjs/common';
import { {{> TEntity}} } from './entities/{{> entity}}.entity';
import { TypeOrmCrudService } from '@nestjsx/crud-typeorm';
import { InjectRepository } from '@nestjs/typeorm';

@Injectable()
export class {{> service}} extends TypeOrmCrudService<{{> TEntity}}>{
  constructor(@InjectRepository({{> TEntity}}) repo) {
    super(repo);
  }
}
